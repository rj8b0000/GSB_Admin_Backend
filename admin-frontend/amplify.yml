version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd admin-frontend
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: admin-frontend/dist
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
